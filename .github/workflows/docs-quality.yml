name: Docs Quality

on:
  pull_request:
    paths:
      - '**.md'
      - '.markdownlint.jsonc'
      - '.vale.ini'
      - 'styles/**'
      - 'cspell.json'
      - 'Windsor.dic'
      - 'lychee.toml'
      - '.lycheeignore'
      - '.github/workflows/docs-quality.yml'

permissions:
  contents: read

jobs:
  markdownlint:
    name: markdownlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-node@v4 # renovate: depName=actions/setup-node
        with:
          node-version: '20'

      - name: Install markdownlint-cli2
        run: npm install -g markdownlint-cli2@0.14.0

      - name: Run markdownlint
        run: markdownlint-cli2 "**/*.md" "#node_modules"

  vale:
    name: Vale (prose)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: errata-ai/vale-action@reviewdog # renovate: depName=errata-ai/vale-action
        with:
          version: 3.7.1
          fail_on_error: true
          filter_mode: nofilter
          reporter: github-check
          token: ${{ secrets.GITHUB_TOKEN }}

  cspell:
    name: cspell (spelling)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-node@v4 # renovate: depName=actions/setup-node
        with:
          node-version: '20'

      - name: Install cspell
        run: npm install -g cspell@8.14.4

      - name: Run cspell
        run: cspell --no-progress "**/*.md"

  lychee:
    name: lychee (link check)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Restore lychee cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: .lycheecache
          key: lychee-${{ github.sha }}
          restore-keys: lychee-

      - uses: lycheeverse/lychee-action@v1.10.0 # renovate: depName=lycheeverse/lychee-action
        with:
          args: --config lychee.toml --no-progress '**/*.md'
          fail: true

  alex:
    name: alex (inclusive language)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-node@v4 # renovate: depName=actions/setup-node
        with:
          node-version: '20'

      - name: Install alex
        run: npm install -g alex@11.0.1

      - name: Run alex
        run: alex --quiet '**/*.md'

  frontmatter:
    name: frontmatter (title + description required)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check every page has title and description
        run: ./scripts/check-frontmatter.sh
